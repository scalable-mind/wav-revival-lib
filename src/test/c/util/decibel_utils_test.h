#ifndef DECIBEL_UTILS_TEST_H
#define DECIBEL_UTILS_TEST_H

/*
 * Checks if hand-calculated values are the same as the output of get_spl().
 */
void DecibelUtils__get_spl__test_1();

/*
 * Checks if hand-calculated values are the same as the output of spl_to_rsp().
 */
void DecibelUtils__spl_to_pascal__test_1();

/*
 * Checks if hand-calculated values are the same as the output of spl_to_ratio().
 */
void DecibelUtils__spl_to_ratio__test_1();

#endif
